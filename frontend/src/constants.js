/**
 * Shared constants — design system and layout.
 * See docs/DESIGN-SYSTEM.md and docs/IMPROVEMENTS.md.
 */

export const NAV_HEIGHT = 56;
export const WRAPPER_MAX_WIDTH = 1160;

/** Badge variants for alternating UI (gold, cosmic, cyan, terra). */
export const BADGE_VARIANTS = ['gold', 'cosmic', 'cyan', 'terra'];
